Goal:
Write a multithreaded program that counts how many prime numbers are in a given range (e.g., 1 to 100,000).
Instead of using a single thread, divide the range into equal chunks and process each chunk in parallel using an ExecutorService.